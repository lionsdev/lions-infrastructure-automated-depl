---
# Titre: Template de ServiceAccount Kubernetes pour PrimeReact
# Description: Définit le ServiceAccount Kubernetes pour une application PrimeReact
# Auteur: Équipe LIONS Infrastructure
# Date: 2025-05-07
# Version: 1.0.0

apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ app_name }}"
  namespace: "{{ app_namespace }}"
  labels:
    app: "{{ app_name }}"
    version: "{{ app_version }}"
    environment: "{{ app_environment }}"
    technology: "primereact"
  annotations:
    description: "ServiceAccount pour l'application {{ app_name }} ({{ app_version }}) en environnement {{ app_environment }}"