---
# Titre: Template de ServiceAccount pour Ollama
# Description: Définit le ServiceAccount pour Ollama
# Auteur: Équipe LIONS Infrastructure
# Date: 2025-05-14
# Version: 1.0.0

apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ app_name }}"
  namespace: "{{ app_namespace }}"
  labels:
    app: "{{ app_name }}"
    version: "{{ app_version }}"
    environment: "{{ app_environment }}"
    technology: "ollama"
  annotations:
    description: "ServiceAccount pour Ollama {{ app_name }} ({{ app_version }}) en environnement {{ app_environment }}"
automountServiceAccountToken: true
