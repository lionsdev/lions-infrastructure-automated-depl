---
# Titre: Template de ServiceAccount Kubernetes pour MongoDB
# Description: Définit le ServiceAccount Kubernetes pour MongoDB
# Auteur: Équipe LIONS Infrastructure
# Date: 2025-05-08
# Version: 1.0.0

apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ app_name }}"
  namespace: "{{ app_namespace }}"
  labels:
    app: "{{ app_name }}"
    version: "{{ app_version }}"
    environment: "{{ app_environment }}"
    technology: "mongodb"
  annotations:
    description: "ServiceAccount pour MongoDB {{ app_name }} ({{ app_version }}) en environnement {{ app_environment }}"